# 一键安装

将两个安装脚本和一个主机 APK 放在同一文件夹，双击 `install-host.cmd`，按提示输入主机 IP。脚本自动准备 ADB、安装应用、停用老化测试并开启自启动。主机需开启网络调试、支持 `su 0`。端口默认 5555，也可输入 `IP:端口`。

命令行可直接指定地址：

```powershell
.\install-host.cmd -Device 192.168.10.210
```
