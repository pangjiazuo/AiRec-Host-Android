# 安卓录像机主机

- 独立于 AiRec-Android 手机客户端，最低 Android 9 / API 28。
- 当前仅局域网，原生后台录像服务与本机界面解耦；模型和运行库随 APK 提供。
- 仅操作任务指定板卡：ADB -s 192.168.10.209:5555。不得刷固件或修改系统分区来掩盖兼容性失败。
- 设备端文件只放应用私有目录或明确的临时诊断目录，保留已有录像与配置。
- 不把缺少信号、模型故障或编码器不足伪装成成功；分别显示通道错误。
- 构建和测试后才报告通过，五个物理摄像头与长期运行测试单独说明。
- 不提交或推送；私钥、缓存、APK、现场画面不进 Git。

## 本机界面与验证

- 主机界面使用 APK 内的 WebView 页面，源文件在 web-src/ 与 app/src/main/assets/web/host.css。修改 JS 后运行 scripts/build-web.ps1；打包前校验 web-files.json。
- 当前板卡 WebView 为 Chrome 66，打包目标需保持 chrome66，不能只以桌面浏览器通过为准。
- 覆盖安装先以 SINGLE_TOP 的 stop intent 停止，并等日志出现新的“采集与片段封装已停止”；安装后确认服务已恢复。不卸载、不删除录像。
- 调试包可通过 captureUi intent 将应用视图存入自身 ui-verification 目录；只用于布局核对，不替代系统窗口或实际播放器验证。读取截图须等写入完成。
- 用户已批准浅色/深色主机设计，页面需与实际 APK 截图对照。禁止把测试数据截图说成现场画面。

## 版本编号（2026-09-14 起）

- 按用户要求，本轮显示版本统一从 **1.0.0** 起。
- 后续小修改只增加最后一位：1.0.0 → 1.0.1 → 1.0.2。只递增实际改动并发布的应用；未获用户要求不自动升级前两位。
- 每次发布内部 versionCode 必须递增，不能随显示版本归零，以保证覆盖升级并保留数据。
- 打包配置、关于页面、日志/接口中暴露的应用版本保持一致；历史验证记录、旧截图与旧产物保留当时版本。

## 1.0.1 本机低负载预览

- 本机实时图像通过 LocalPreview 的 SurfaceView 直接接入采集 EGL；WebView 只负责界面与位置上报。网络 MJPEG 仍独立提供，统计观看连接数并在断开时释放。
- 无网络观看者时，JPEG 只服务信号检测、识别与截图；不能把低频 JPEG 当成本机预览帧率，也不能把主进程 CPU 当作整个 APK 占用。CPU 对比包含主进程、WebView 和 detector，并保持录像与识别配置一致。
- Surface 销毁前先在采集线程解除 EGL 绑定；切页、弹窗、后台和恢复需实测。主机实际画面截图用设备 screencap，旧 web.draw 只包含网页，不包含原生视频表面。

## 1.0.5 马赛克录像

- 用户接受间隔定位、逐帧图像跟踪的取舍：快速新目标可能短暂漏遮挡；三端确认提示必须保留这一说明。
- PrivacyTracker 保留小图历史以对齐异步定位结果，NativePrivacy 用独立模型定位和小图纹理跟踪，PrivacyShader 在硬件编码前遮挡。关闭时不运行这条处理链。
- 验收读取实际 MP4 的帧数与时间戳，不用重复帧凑 15 FPS；平均帧率、低谷、五路实拍与长期保证分别说明。
- RockX SDK 的公开分发授权尚未确认，其模型和运行库继续忽略提交；不能把本地测试 APK 当作已获公开分发许可。
